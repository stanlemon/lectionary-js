# Lectionary Calculator

This is a library for calculating weeks of the historic Christian lectionary calendar.

To get started:

Install dependencies:

```shell
npm install
```

Run tests:

```shell
npm run test
```
